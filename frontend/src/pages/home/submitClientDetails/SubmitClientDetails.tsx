import { Typography } from "@mui/material";
import React from "react";

export const SubmitClientDetails = (): JSX.Element => {
    return (
        <Typography variant="h3" color={"white"}>
            Submit client details!
        </Typography>
    );
};
